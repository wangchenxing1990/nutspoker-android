<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置几何图形四个角的弧度 -->
    <corners android:radius="@dimen/recent_prompt_radius" />
    <!-- 设置填充颜色 -->
    <solid android:color="@android:color/transparent" />
    <!-- 设置几何形状绘制边框 红色边框 -->
    <stroke
        android:width="@dimen/recent_prompt_stroke_width"
        android:color="@color/shop_text_no_select_color" />
</shape>