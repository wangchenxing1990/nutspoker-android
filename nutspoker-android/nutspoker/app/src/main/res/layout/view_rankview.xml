<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rl_rankview"
    android:layout_width="@dimen/rank_view_width"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:layout_centerInParent="true"
    android:gravity="center">

    <ImageView
        android:id="@+id/iv_rank"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/mtt_room_rank_1"/>

    <TextView
        android:id="@+id/tv_record_match_rank_tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/match_rank_common_bg"
        android:includeFontPadding="false"
        android:textColor="#FF924C"
        android:textSize="12dp"
        android:visibility="gone"
        android:drawablePadding="2dp"
        tools:visibility="visible"
        tools:text="2"
        android:layout_centerInParent="true"
        android:lines="1"
        android:ellipsize="end"
        android:layout_gravity="center"
        android:gravity="center" />

</LinearLayout>